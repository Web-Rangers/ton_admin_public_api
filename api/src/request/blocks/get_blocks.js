async function get_blocks(offset_id=0,limit=100) {
    
    // limit = Number(limit)
    // offset_id = Number(offset_id)
    // limit = limit <=100?limit:100
    // let blocks = (await Block.where({})).slice(offset_id,(offset_id+limit)).map(x=>x={transactions:x.transactions,height:x.height})
    return []
}

export {get_blocks}